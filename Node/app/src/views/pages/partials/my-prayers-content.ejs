<main class="dashboard-page prayer-page" data-prayer-page>
  <section class="section dashboard-section">
    <div class="container info-container">

      <div class="info-grid prayer-grid">
        <form class="dashboard-card prayer-form" data-prayer-form>
          <div class="prayer-card-intro">
            <h2>Share a prayer request</h2>
            <p>Add a clear request for the community. You can mark it answered later.</p>
          </div>
          <label class="auth-field prayer-field">
            <span>Prayer request</span>
            <textarea name="prayer" rows="7" maxlength="500" placeholder="" required></textarea>
          </label>
          <button class="btn btn-primary auth-submit" type="submit">Share request</button>
          <p class="dashboard-answer-status prayer-form-status" data-prayer-status aria-live="polite"></p>
        </form>

        <section class="dashboard-card prayer-list-card">
          <header class="dashboard-card-head">
            <h2>Active prayer requests</h2>
            <span data-active-prayer-count>
              <%= prayerLists.active.length %> items
            </span>
          </header>
          <%- include("./prayers-table", { prayers: prayerLists.active, showActions: showPrayerActions,
            listType: "active" }) %>
        </section>
      </div>
    </div>
  </section>

  <%- include("../../components/layout/footer") %>
</main>