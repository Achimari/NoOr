<% const cardTag = typeof tag === "undefined" ? "div" : tag; %>
<% const cardClassName = typeof className === "undefined" ? "" : className; %>
<%- `<${cardTag} class="card${cardClassName ? ` ${cardClassName}` : ""}">` %>
  <%- body %>
<%- `</${cardTag}>` %>
